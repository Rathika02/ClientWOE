# Screenshots to capture

Take these from your own machine while the API, SQL Server and frontend are running, and save them in this folder:

| File name | What to show |
|---|---|
| `01-order-entry.png` | Frontend with a client, patient details and two or more tests added (total visible) |
| `02-saved-woe.png` | The "Work order saved" screen with the WOE number |
| `03-swagger-post.png` | Swagger `POST /api/workorders` request and the `201` response |
| `04-validation.png` | Inline validation errors (no tests / invalid mobile) |
| `05-sql-rows.png` | `SELECT * FROM WorkOrder; SELECT * FROM WorkOrderTestDetail;` results in SSMS |

Then uncomment or add the image links in the root `README.md`.
